version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'npm install'
        build:
            commands:
                - 'npx expo export -p web'
    artifacts:
        baseDirectory: dist
        files:
            - '**/*'
    cache:
        paths:
            - 'node_modules/**/*'
